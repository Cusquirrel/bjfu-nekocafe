# Accessibility

- `axe-accessibility.spec.ts` 使用 Playwright + axe 检查严重和关键可访问性问题，并输出 `tests/reports/axe-results.json`。
- `lighthouse-home.ps1` / `lighthouse-home.sh` 输出 `tests/reports/lighthouse-home.html`。
